name: Nodejs test
on:
 push:
  branches: 
   - main
 pull_request:
   branches: 
    - main
jobs:
 something:
  runs-on: ubuntu-latest
  steps:
    - name: checkout code
      uses: actions/checkout@v3
    
    - name: install nodejs and run test
      uses: actions/setup-node@v4
      with:
       node-version: 18
    - name: Install dependencies
      run: npm install
    - run: npm ci
    - run: npm test
    
      
    
